// create a new controller and pass it the $scope variable
var MyController = function($scope){

  //$scope holds the values for all our models, so set an initial value on the qty model
  $scope.qty = 1;
};